---
description: "Compile raw sources into wiki articles (--force to recompile all)"
---

Use the wiki skill to compile raw sources into wiki articles.

- `/wiki:compile` — Compile new/changed sources only
- `/wiki:compile --force` — Recompile all sources
- `/wiki:compile --dry-run` — Show what would be compiled

/wiki compile $ARGUMENTS
